<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body class="bg-gray-100 flex justify-center items-center min-h-screen min-w-full">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full m-10">
        <h1 class="font-bold text-2xl mb-6 text-center">Register Here</h1>
        <form action="/submit" method="POST" class="space-y-4">
            <!-- Name Input -->
            <div>
                <label for="name" class="block font-medium text-gray-700">Name</label>
                <input type="text" id="name" name="name" placeholder="Your Name"
                    class="w-full p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-indigo-300">
            </div>
            <!-- Text Area Input -->
            <label for="name" class="block font-medium text-gray-700">Details</label>
            <textarea type="text" id="name" name="details" placeholder="Your text"
                class="w-full resize-none p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-indigo-300"></textarea>

            <!-- Submit Button -->
            <div>
                <button type="submit"
                    class="white_text w-full bg-indigo-600 text-white font-bold py-2 rounded-lg hover:bg-indigo-700 transition">
                    Submit
                </button>
            </div>
        </form>
        <div class="min-w-full flex gap-4 items-center justify-center  overflow-auto p-4">
            <% if (files.length> 0) { %>
                <% files.forEach((value)=> { %>
                    <div
                        class="min-w-52 bg-red-500 min-h-32 bg-black text-white rounded-lg shadow-md mt-4 flex flex-col justify-between px-3">
                        <h1 style="color: white;" class="text-center font-bold text-lg p-4 truncate">
                            <%= value %>
                        </h1>
                        <div class="flex items-center justify-between  py-2">
                            <a style="color: white; background-color: blue;" href="/files/<%= value %>" class="text-blue-500 hover:underline px-2 py-1 rounded-lg">
                                Read More...
                            </a>
                            <a style="color: white; background-color: green;" href="/edit/<%= value %>" class="text-indigo-500 hover:underline px-2 py-1 rounded-lg">
                                Edit
                            </a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <p class="text-gray-500 text-center font-medium">No tasks yet</p>
                            <% } %>
        </div>

    </div>
</body>

<script src="/script/script.js"></script>

</html>